# Taste
- Communicates with the agent in Chinese (Simplified); reports bugs in natural-language, user-visible symptom terms rather than technical wording, and expects the agent to trace the root cause. Confidence: 0.8
- Prefers minimal, curated settings UI over exhaustive customization — explicitly pushes back on feature-rich config screens ("别提供那么多自定义选项"), wanting only the meaningful options exposed. Confidence: 0.7
- Points the agent at a concrete existing app/reference project to emulate its behavior and data sources, rather than describing the desired design abstractly. This is a recurring pattern, not a one-off: for later features they again hand over a repo link as the starting point (e.g., pointed at the `andelby/widbar` GitHub project to show how to embed into the taskbar). Confidence: 0.7
- Rejects fixes that trade one working behavior for another — a fix that resolves the reported bug but breaks existing functionality is treated as a failure. Expects the agent to keep hunting for a solution that satisfies both constraints (and to research the real mechanism when needed) rather than settling for a compromise. Confidence: 0.65
- Treats the user-visible symptom as the source of truth over the agent's own tests: if the artifact still visibly misbehaves, they re-report it tersely and emphatically ("还是会闪！现在是切换过程中闪！！！" / "切换过程还是闪白色！！！！！！" / "还是会被搞到下面去。。。。" / "为什么还是不会启动排开！！！！" / "开启任务栏嵌入怎么还是没反应啊！！！！！") and expect the agent to keep digging for the residual root cause instead of declaring victory off a passing test — even after the agent ran a self-constructed "decisive" experiment and called it verified. The emphasis level is a signal: the more exclamation marks, the more the agent's prior "verified/完成" claims are being rejected. Confidence: 0.9
- Wants the definitive, root-cause fix rather than another incremental patch once an issue keeps recurring — explicitly asks for "一个完美方案" and grants permission to refactor ("必要的时候可以重构"). Do not offer band-aids or list of speculative options; if iterative fixes have failed, step back and fix the underlying mechanism. Confidence: 0.7
- Expects temporary diagnostic scaffolding (probe/verification scripts, temp instrumentation, throwaway build output) to be cleaned up once the fix is confirmed — explicitly asks for it ("可以清理探针") rather than leaving it in the tree. Confidence: 0.55
- Rejects having a bug attributed to their own environment/setup — when the agent hypothesizes the OS/settings are misconfigured, they push back emphatically ("我肯定没有禁用啊！") and expect the agent to own the root cause in the code instead of blaming the user's configuration. Confidence: 0.5
- Wants visual appearance to be user-switchable: expects a control (e.g., a setting) to toggle between looks rather than a single hardcoded style, while keeping the current look as the default/opt-in baseline (e.g., "目前是 Apple 风格，请你允许切换风格"). The minimal-settings preference applies to gratuitous customization, not to options directly tied to a feature they asked for — when offered sub-options for the requested feature they chose to expose both (Acrylic + Mica). Confidence: 0.5
- Expects hardcoded behavior to be turned into a user-facing setting on request ("请你允许设置/切换…"): after style switching, next wants island placement switchable between screen top and bottom, and spells out the concrete expected behavior of each mode (bottom mode embeds in the taskbar, slides over, queue grows upward) rather than accepting a bare toggle. Confidence: 0.5
- Expects visual uniformity/alignment across repeated UI elements rather than per-element sizing: reports mismatched sizes emphatically ("每张卡片的大小都不一样！！你应该同一这个行为") and wants the host to normalize them (e.g., cards in a stack forced to one shared width/height). Confidence: 0.55
- Expects alignment to propagate to inner content and to the growth direction: when a component is placed at the left/right edge, its popups must be left/right-aligned to that same side and expand only away from the edge — center-alignment makes wider content grow symmetrically and overflow off-screen ("弹出的消息应该左对齐/右对齐，中间对齐会导致溢出"). Confidence: 0.6
- Wants the widget to occupy space in the host shell's native layout rather than float as an overlay on top of it — overlapping the existing UI (e.g., the taskbar's own icons) is treated as a defect, so prefer placements that avoid occlusion or that integrate with the shell. Confidence: 0.55
- Expects element size to be uniform across contexts/position modes and never a function of where the element sits — but does not accept deleting a requested behavior to achieve that. An early revision narrowed the compact width only inside the taskbar ("嵌在任务栏变窄、出去又变宽"), which they called weird ("尺寸只由内容决定"); when the agent responded by removing the narrowing altogether, they rejected that too ("为什么…嵌入的时候也没有缩小啊！！！！"). The wanted fix is a single, context-independent size cap applied in every position mode — not removal of the requested behavior. Any size constraint must be a fixed constant, not derived from the current placement. Confidence: 0.7
- Prefers the real integrated implementation over the pragmatic workaround even when it means an invasive, large-scope, not-guaranteed-to-succeed project: when offered a zero-risk route (have the user free up taskbar space via OS settings) against a native explorer-hook injection explicitly labeled "大工程/不保证成功", they took the injection. Don't default them to the easy concession when genuine integration requires invasive work. Confidence: 0.55
- Requires system-invasive changes to be fail-safe and reversible: before approving anything that hooks or alters another process (e.g., injecting a DLL into explorer.exe) they demanded 失败自动回滚 — on any failure the change must undo itself and fall back to the previously working behavior rather than leaving the system/shell in a broken state. Confidence: 0.5
Remote HTTP/network access: this machine cannot directly resolve/reach GitHub content (raw.githubusercontent failed, and `git push` to github.com repeatedly fails with a TLS-handshake reset — SNI-style blocking; no proxy is running by default even though Clash Verge is installed, and a `hub.tubawinui3.cn` accelerator mirror in gitconfig no longer resolves). Reach remote sources through the agent's own web_search/web_fetch channel, prefer authoritative on-machine sources (e.g., the installed Windows SDK headers under Windows Kits) over copying a third-party reference project, and treat remote pushes as likely to fail (diagnose connectivity and offer a fallback) rather than assuming success. The user can be asked to turn their proxy on when needed — they will do it and reply tersely ("打开了代理。") — after which a local Clash Verge HTTP proxy is listening at 127.0.0.1:7890 and routing git through it per-command (`-c http.proxy=http://127.0.0.1:7890 -c https.proxy=...`) makes the push succeed; route through the proxy without changing the user's global git config. Confidence: 0.5
- Accepts clearly-stated, unavoidable residual costs of the real implementation rather than demanding a fully reversible solution: when told the injected hook DLL can never be unloaded from explorer and that updating it requires restarting explorer, they replied simply "可以接受。" and expected work to continue. The fail-safe/reversibility demand applies to behavior and rollback, not to a technically unavoidable residue. Confidence: 0.5
- Approaches a 1.0 release as an explicit planning exercise: asks the agent to analyze and enumerate what still has to be completed before shipping ("请你分析，再完善哪些东西可以发布 1.0 版本"), expecting a prioritized, categorized gap list (must-do vs. recommended vs. deferrable, with a suggested order) instead of the agent jumping straight into implementation. Confidence: 0.5
- Will cut an invasive approach loose rather than carry it forward once it proves fragile or only half-delivering: after the explorer-injection hook produced only partial results and left permanently-loaded DLLs in explorer, they had the whole subsystem removed ("请你删掉这个hook功能。"). Their appetite for invasive integration is conditional on it actually delivering, not a standing license for risky work — and they will reverse an earlier approval when the payoff doesn't materialize. Confidence: 0.5
- Uses Inno Setup for Windows installer packaging and GitHub Actions for the CI release pipeline; expects release builds to ship separate x64 and ARM64 installers. Confidence: 0.6
- Treats committing and pushing to the remote as part of finishing a task, not a separate request: after an implementation is done they say "写完之后推送一下代码。" and expect the agent to inspect repo state, commit the changes and push to origin without further prompting. Confidence: 0.6
- Treats plugin-source changes as incomplete until the marketplace package is republished: given a terse "改了请帮我重新搞成lwp提交pr到GitHub", they expect the agent to verify what actually changed, bump the plugin version, rebuild/package to `.lwp`, and open a PR against the marketplace repo on its own — the delivery path is source repo → built package → marketplace PR, handled end-to-end. Confidence: 0.6
- Treats pushing followed by triggering a CI build as the standard finishing step and gives it as a single terse instruction ("推送然后触发一次构建。"): after pushing they expect the agent to kick off the release workflow (e.g., via the GitHub CLI) and watch the run to completion, not just stop at the push. Confidence: 0.55
- Once a recurring bug class is diagnosed, expects the constraint to be encoded into the generator artifacts — the agent skills, plugin-project templates, and reference docs — not just fixed in the current code, so future generated projects can't repeat the mistake ("那你得把这个约束加在插件生成的skills里面！！！"). This includes keeping templates in sync (e.g., shipping the required `global.json` in the plugin template so generated projects are correct by default). Confidence: 0.7
- Skills must be edited in their source repository, never in the locally installed copy: the installed `~/.agents/skills/` directory is a build artifact, so skill changes belong in the source repo (e.g., `~/Documents/WinLandPluginSkills` → `luolangaga/WinLandPluginSkills`) and presumably flow out from there. Reaffirmed emphatically when the agent began editing the wrong copy — the user cut in tersely with the exact repo path and exclamation marks ("C:\\Users\\luolan\\Documents\\WinLandPluginSkills是这个！！！") and expected the already-written skill changes to be redone there. Confidence: 0.85
- Editing a skill in its source repo is not "done" until the locally installed copy is refreshed from that source: they explicitly ask for the local install to be updated too ("然后给我吧本机安装的副本给更新了"), so treat repo edit → sync to `~/.agents/skills/` as one workflow, not two separate requests. Confidence: 0.6
- Prefers superseded/deprecated skill versions removed outright rather than left to coexist with the current one: a stale skill still teaching the old API (e.g., the 1.x `winland-plugin-dev`) will "带偏" future sessions, so they say to delete it ("删掉1.0那个") instead of keeping it alongside the new skill. Confidence: 0.55
- Expects presentation-grade project documentation, not a bare text README: asks for a "高级" README with illustrative images and explicitly authorizes the agent to obtain the assets itself ("你可以自己截图帮我配图") — run the app, capture the real UI, and embed the screenshots rather than asking the user to supply them. Confidence: 0.55
- When a new feature supersedes an existing interaction, expects the old logic deleted rather than left in place alongside the new one ("删掉之前那些点击的逻辑") — the capability may move into the new surface as an explicit control, but the stale handlers/entry points must not survive. Confidence: 0.6
- Expects new interaction surfaces to be animated in both directions, with visual continuity from the element that triggered them: an open animation implies a matching reverse animation ("点击边框外部可以收回，依旧也有动画"), and the surface should fly in from the current on-screen position of the widget and scale up to center rather than just fade in place. Confidence: 0.6
- Demands animations read as ONE continuous morph — any perceptible intermediate "switching" step between two visuals is a defect, reported tersely and emphatically ("切换的过程不连贯！！感觉中途有明显的切换流程！"). The detail card should take over the widget's exact rectangle/corner radius and grow into place as if it were the widget itself deforming, instead of two separate visuals crossfading. Applies to both directions of the transition. Confidence: 0.6
- Wants detail/large cards to feel genuinely premium and effect-rich, not merely functional: explicitly asks for a more high-end card design, scroll-fill/rolling lyric effects, and animated backgrounds that react to live data (e.g., a bar pulsing to the music's rhythm — "大卡设计的能不能更高级一点？歌词滚动效果那些。以及背景动效…根据音乐律动的条"). Prefers subtle continuous motion driven by real state (audio levels, charge rate) over static presentation. Confidence: 0.5
- Prefers a tiered display: the small widget stays compact while richer data (more fields, detail streams) is surfaced in a separate, larger, deliberately beautiful card ("设计一个无比好看的大岛，并且展示更详细的信息"), with the feature owning the content/size and the host owning chrome, animation and dismissal ("何时打开由插件自己决定…里面的内容也是由插件设定"). Prefer building that detail surface over cramming more elements into the existing widget. The minimalism applies to EVERY intermediate surface, not just the compact state: they complained the charging island's expanded panel carried too much data ("充电大岛的数据是不是给的太多的，那些信息超大岛展示就行了啊！！") and had detail fields (capacity/health/cycles/estimated time) pulled out of the expanded island so they appear only in the large spotlight card, leaving the expanded state with just the at-a-glance values. Confidence: 0.7
- Treats new feature/API conventions as something to propagate into the plugin-authoring skills repo, not just fix in code: "把这个规则写入插件制作的skills里面" — deliver the feature together with the rule update in the skills source repo. This recurs: after shipping a new host capability (e.g. the drag-drop island) they follow up with "请你吧这个拖拽岛的功能写到插件开发的skills里面", i.e. the feature isn't finished until its new API and behavior are documented in the plugin-dev skill (new section + API table entry). Confidence: 0.65
- Freezes an approved visual as the baseline and wants it restored wholesale once later tweaks make it worse: after iterative "improvements" they say "你还是改回第一版吧，怎么越改越丑了。。。" and expect the whole previous version (look, size, animation) reverted exactly, not another round of partial fixes. The last version they explicitly accepted is the reference point, and further polish should not be attempted unasked. Confidence: 0.6
- Expects routine interactions and key info to be available directly in the main island, not gated behind opening the larger detail card: being forced to open the big card just to change position is reported as a defect ("要切换进度还得点开超大岛"). Interactive elements (e.g., the progress/seek bar) must be clickable/draggable in the compact-and-expanded island itself, with the big card reserved for extra detail rather than a prerequisite for basic operations. Confidence: 0.6
- Expects a capability added to one plugin's surface to be propagated to the other plugins' surfaces for consistency: right after the media island gains scrubbable progress they ask the battery plugin's island be upgraded to match ("充电插件也适配一下这个大岛"), then follow up asking for the same adaptation on the battery plugin's large spotlight card ("继续适配我的充电岛的超大岛"). Treat feature work as applying across all plugin surfaces — compact island, expanded island, and the large detail/spotlight card — not only the one explicitly named. Confidence: 0.6
- States animation fluidity as a hard requirement of the feature request itself, emphatically and up front ("动画要流畅！！！") — smooth, continuous motion (velocity lerped rather than hard-switched, composition-level animations) is expected as part of the deliverable, not a later polish pass. Confidence: 0.6
- Wants new host capabilities to be extensible by plugins rather than hardcoded: the request itself specifies the content list must be plugin-loadable ("可由插件加载"), so design the surface with a host-side shell plus a plugin registration API from the start. Confidence: 0.5
- Expects a new interaction surface to cover the full range of comparable content/payload types, not just the one first implemented: after file drag-drop ships they immediately ask for the other kinds ("请你加入其他内容拖入的效果，比如文字，图片等等"), having earlier asked whether different extensions could trigger different actions. Treat the first cut of a feature as the starting scope and proactively generalize it to all analogous variants. Confidence: 0.5
- Prefers unsupported/inapplicable options to be removed from the UI entirely rather than shown disabled or dimmed: on seeing incompatible drop targets rendered as greyed-out cards they objected ("这个格式不支持的选项就不应该展示而不是现在这样的禁用"), wanting the surface to show only what is currently actionable. Design UIs to hide options that don't apply to the current context, not to disable them in place. Confidence: 0.7
- Challenges user-facing options that appear to overlap or duplicate each other ("载入历史和复制路径不都是一个东西吗？？？？") and, when the overlap can't be justified, resolves it by deletion rather than by better labeling: after asking what distinguishes the plugin's "记入历史" drop card from the host's built-in "复制路径", they simply order the redundant one gone ("你应该把记入历史删掉"). Don't ship two similarly-named actions; where one already covers the need, remove the duplicate surface outright and expect the now-unused dependencies/version requirements (e.g. `min_host_version`) to be walked back with it. Confidence: 0.6
- Expects a content-capture surface to cover all the relevant content kinds rather than an initial subset: when the clipboard island only handled text and images they asked whether it could support files too ("剪切板插件可以支持文件吗？？？？"), i.e. clipboard capture should span text, images, and files/folders. Confidence: 0.5
- Requires captured content to round-trip at full fidelity: whatever the feature captured must be replayed back in its native form, not a degraded/lossy representation. A copied file must paste back as a file (its native clipboard format, e.g. CF_HDROP), not as a path string — "我希望我复制出来的文件通过剪切历史粘贴出去的也应该是文件啊！不应该是文件路径". Treating a type-faithful payload as "good enough to store as text" is a defect, even when the text form is technically usable. Confidence: 0.6
- Expects status/feedback text to accurately describe the action that just happened, not a generic or hardcoded label: seeing the island toast "已复制" after they had pasted (picked a history item) is flagged as a bug ("为什么粘贴提示的是已复制"). Feedback messages should be derived from the actual operation performed (copy vs. paste), not a single fixed string reused across actions. Confidence: 0.5
- Verifies features in real third-party applications, not just the plugin's own UI: they tested the clipboard file-paste by pasting into a chat app ("我粘贴到QQ里面的"), so behavior must be validated against how actual target apps (chat windows, editors, Explorer) consume the payload — a copy that only works in the agent's own surface is treated as broken. Confidence: 0.5
ckage publication as part of a shared library's delivery. Confidence: 0.55
- Cares about NuGet package correctness: expects the packed contents and nuspec metadata (id, version, license, tags, dependencies, and especially the repository/project URL) to be verified against reality before upload — a guessed repo URL that 404s (e.g. `github.com/luolan/winland` instead of the real `github.com/luolangaga/WinLand`) is treated as a defect to fix and repackage. Confidence: 0.55
- Publishes under the GitHub account/org `luolangaga` (e.g. `luolangaga/WinLand`, `luolangaga/WinLandPluginSkills`); that real remote is authoritative over any placeholder URL written into project metadata. Confidence: 0.6
- Prefers a shared library/SDK to be consumed as a published NuGet package rather than by referencing the host source repo: once the package went live they immediately had the skill's install flow rewritten to be NuGet-first ("请你把skill的安装流程改成nuget！！！"), demoting the source `ProjectReference` to an optional path used only when modifying the SDK itself. The consumer path should require no source checkout, and the switch must be carried through every artifact consistently (template csproj, skill steps, env-check script, README). Confidence: 0.6
- Expects the *published* artifact itself to be the source of truth, not local build output: when the agent diagnosed a bad NuGet package from local files, they pushed back and told it to fetch the real package from the registry and re-check ("你再拉下来看看？？？"). Verify claims about what was shipped by downloading/unpacking the actual deployed artifact, and distinguish "my local copy" from "what is live" before reporting a verdict. Confidence: 0.5
