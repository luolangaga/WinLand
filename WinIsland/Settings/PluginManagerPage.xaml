<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="WinIsland.Settings.PluginManagerPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel Spacing="8">
        <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="插件管理" />
        <TextBlock
            Margin="0,0,0,12"
            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
            Style="{StaticResource CaptionTextBlockStyle}"
            Text="插件以 plugins/&lt;id&gt;/ + plugin.json（或 .lwp 插件包）形式提供，启用/禁用会即时生效且不留残留"
            TextWrapping="Wrap" />

        <InfoBar
            x:Name="WarningBar"
            IsClosable="True"
            IsOpen="False"
            Severity="Warning"
            Title="插件目录提示" />

        <InfoBar
            x:Name="ResultBar"
            IsClosable="True"
            IsOpen="False" />

        <Border Style="{StaticResource SettingsCardStyle}">
            <Grid ColumnSpacing="16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Spacing="2" VerticalAlignment="Center">
                    <TextBlock Text="插件目录" />
                    <TextBlock
                        x:Name="PluginsPathText"
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                        Style="{StaticResource CaptionTextBlockStyle}"
                        Text=""
                        TextWrapping="Wrap" />
                </StackPanel>
                <StackPanel
                    Grid.Column="1"
                    Orientation="Horizontal"
                    Spacing="8"
                    VerticalAlignment="Center">
                    <Button
                        x:Name="InstallButton"
                        Click="Install_Click"
                        Content="安装插件包..." />
                    <Button
                        x:Name="OpenFolderButton"
                        Click="OpenFolder_Click"
                        Content="打开文件夹" />
                </StackPanel>
            </Grid>
        </Border>

        <TextBlock
            Margin="0,16,0,4"
            Style="{StaticResource SubtitleTextBlockStyle}"
            Text="已安装插件" />

        <StackPanel x:Name="PluginList" Spacing="6" />
    </StackPanel>
</UserControl>
